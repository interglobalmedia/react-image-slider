import React from 'react';
import Slider from './Slider';

describe('<Slider />', () => {
	it('should render a slide, a left and right arrow, and slide indicators', function() {
		expect(<Slider />).toEqual(<Slider />);
	})
})
