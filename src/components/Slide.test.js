import React from 'react';
import Slide from './Slide';

describe('<Slide />', () => {
	it('should return an image whose index equals the activeIndex and it should return a string escribing the image contents',() => {
		let activeIndex = activeIndex;
		let index = activeIndex;
		let creditsIndex = activeIndex;
		index === activeIndex && creditsIndex === activeIndex ?
			<Slide />
			: null;
	})
	expect(<Slide />).toEqual(<Slide />);
})
